/**
 * useDataProvider hook
 * Provides direct access to the data provider for custom data operations
 * 100% API-compatible with react-admin
 *
 * This is a re-export from contexts for convenience
 */

export { useDataProvider } from '../contexts/DataProviderContext'
